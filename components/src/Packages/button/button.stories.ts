import { CustomButton } from "../button/button";

export default {
    title: "Components/Button",
    component: CustomButton,
}

export const Button = {
    args: {
        buttonLabel: "test"
    }
}